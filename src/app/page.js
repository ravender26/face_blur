"use client";

import LandingPage from "../components/LandingPage";

/**
 * Root Landing Page (route: /).
 * Renders the product capabilities landing page and CTA dashboard triggers.
 */
export default function Home() {
  return <LandingPage />;
}
